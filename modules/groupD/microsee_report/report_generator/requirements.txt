# Pinned runtime dependencies — regenerate with:
#   pip freeze | grep -E "^(pandas|numpy|pydantic)==" > requirements.txt
#
# pyproject.toml is authoritative for compatible ranges (>=x,<y).
# This file pins exact versions for reproducible container builds and CI installs.
# Update after any intentional dependency upgrade.
pandas==2.3.3
numpy==2.4.1
pydantic==2.13.4
